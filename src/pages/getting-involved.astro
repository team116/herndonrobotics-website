---
import Layout from "../layouts/Layout.astro";

const info = [
  {
    id: "new-member",
    title: "New Member",
    contact: "student-interest@herndonrobotics.org",
    instructions: [
      "Check the calendar (under Current Members)",
      "Show up to a meeting",
      "Join our forum/Slack",
      "Complete shop training",
    ],
    involvement: "Join us and start building robots and learning STEM!",
    expectations: [
      "Learn STEM",
      "Build Robots",
      "Grow your skills",
      "Meet like-minded people",
      "Meetings Tuesdays & Thursdays 7-9PM"
    ],
  },
  {
    id: "new-mentors",
    title: "New Mentor",
    contact: "mentors@herndonrobotics.org",
    instructions: [
      "Email our leadership for more details",
    ],
    involvement: "Guide the next generation and experience their growth!",
    expectations: [
      "Teach Students",
      "Advise Members",
      "Meetings Tuesdays & Thursdays 7-9PM",
      "Search 'FIRST Mentor Manual' for more details"
    ],
  },

  {
    id: "plan-outreach",
    title: "Outreach",
    contact: "leadership@herndonrobotics.org",
    instructions: [
      "Contact us with your questions",
      "Set up an event with us",
    ],
    involvement: "Outreach brings STEM programs to expose our community to the world of robots",
    expectations: [
      "Robotics Event",
      "We will work with you set up a time and date",
    ],
  },

  {
    id: "new-sponsor",
    title: "Sponsor",
    contact: "leadership@herndonrobotics.org",
    instructions: [
      "Contact us for info about how to support us"
    ],
    involvement: "",
    expectations: [
      "Furthering STEM education",
      "Team materials and expenses"
    ],
  },
];
---

<Layout title="Getting Involved" single_screen={true}>
  <div
    class="m-4 sm:m-8 flex-1 flex flex-col gap-4"
    x-data={`{active: window.location.hash.slice(1) || '${info[0].id}'}`}
    x-init={`window.addEventListener('hashchange', () => { active = window.location.hash.slice(1) || '${info[0].id}' })`}
  >
    <div class="flex justify-center gap-4 font-audiowide  align-middle items-center text-3xl lg:text-5xl">
      {
        info
          .map((card) => (
            <a href={`#${card.id}`}>
              <div
                class="relative text-center after:absolute after:bottom-0 after:left-0 after:h-0.5 after:w-full after:origin-bottom-right after:scale-x-0 after:bg-primary after:transition-transform after:duration-300 after:ease-in-out hover:after:origin-bottom-left hover:after:scale-x-100"
                x-bind:class={`{'after:scale-x-100': active=='${card.id}'}`}
              >
                {card.title}
              </div>
            </a>
          ))
          .flatMap((item, index, array) =>
            index < array.length - 1 ? [item, <h1 class="hidden sm:block">|</h1>] : [item],
          )
      }
    </div>
    <div
      class="border border-accent rounded-2xl p-8 bg-secondary flex flex-row transition-all gap-8 flex-1"
    >
      {
        info.map((card) => (
          <div
            x-show={`active === '${card.id}'`}
            x-cloak
            class="flex flex-col gap-6 flex-1"
          >
            <div>
              <p class="text-6xl">{card.involvement}</p>
              <br />
              <div class="mt-2 text-5xl flex flex-row flex-wrap items-end">
                <strong>Contact:&nbsp;</strong>{" "}
                <span class="inline sm:hidden"><br></span>
                <a
                  href={`mailto:${card.contact}`}
                  class="relative text-4xl after:absolute after:bottom-0 after:left-0 after:h-0.5 after:w-full after:origin-bottom-right after:scale-x-0 after:bg-primary after:transition-transform after:duration-300 after:ease-in-out hover:after:origin-bottom-left hover:after:scale-x-100"
                >
                  {card.contact}
                </a>
              </div>
            </div>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-16 my-auto p-8 sm:p-16 h-min">
              <h3 class="font-audiowide text-primary-foreground w-fit text-6xl">
                Getting Involved
              </h3>
              <h3 class="font-audiowide text-primary-foreground w-fit text-6xl">
                What to Expect
              </h3>

              <ol class="list-decimal list-inside space-y-2 text-5xl row-2">
                {card.instructions.map((step) => (
                  <li>{step}</li>
                ))}
              </ol>

              <ul class="list-decimal list-inside space-y-2 text-5xl">
                {card.expectations.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</Layout>
